<Window x:Class="RateMe.View.Windows.ApisWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:RateMe.View.Windows"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen" Height="170" Width="320"
        Background="#10151b">
    
    <WindowChrome.WindowChrome>
        <WindowChrome />
    </WindowChrome.WindowChrome>
    
    <Window.Resources>
        <Style x:Key="Content" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="White"/> 
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontFamily" Value="Bahnschrift SemiCondensed"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Height" Value="22"/>
        </Style>
        
    </Window.Resources>
    
    <Grid x:Name="WindowGrid">
        <Grid x:Name="MainGrid" Margin="0,32,0,0" HorizontalAlignment="Center" Width="250">
            <StackPanel Orientation="Vertical" Margin="0,15,0,0">
                <TextBlock Style="{DynamicResource Content}" Text="{Binding MainApiAvailability}"/>
                <TextBlock Style="{DynamicResource Content}" Text="{Binding S3ServiceAvailability}"/>
            </StackPanel>
            
            <Button Style="{StaticResource ResourceKey=ContinueButtonStyle}" Click="OnOkClick" Margin="0,50,0,0"
                    Content="Ок" VerticalAlignment="Center" FontSize="16" FontFamily="Bahnschrift SemiCondensed"
                    Height="30" Width="80" Foreground="White" Background="#032169"/>
        </Grid>
    </Grid>
</Window>
