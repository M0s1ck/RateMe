<Window x:Class="RateMe.View.Windows.GradesWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RateMe.View.Windows"
        mc:Ignorable="d"
        
        xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2"
        Title="GradesWin" Height="860" Width="1600" WindowStartupLocation="CenterScreen"
        Background="#10151b">
    

    <WindowChrome.WindowChrome>
        <WindowChrome />
    </WindowChrome.WindowChrome>

    <Grid x:Name="windowGrid">
        <Grid x:Name="MainGrid" Margin="0,60,0,0" HorizontalAlignment="Center">

            <Grid.Resources>
                <DataTemplate x:Key="FormulaTemplate">

                    <DataGrid ItemsSource="{Binding FormulaObj1}" AutoGenerateColumns="False" BorderBrush="White" Background="Transparent" Foreground="White" HeadersVisibility="Column">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Элемент" Binding="{Binding Name}" Width="20" FontSize="17" />
                            <DataGridTextColumn Header="Вес" Binding="{Binding Weight, StringFormat={}{2f}}" Width="20" />
                            <DataGridTextColumn Header="Оценка" Binding="{Binding Grade}" Width="20" />
                        </DataGrid.Columns>

                        <DataGrid.Resources>
                            <Style BasedOn="{StaticResource {x:Type DataGridColumnHeader}}" TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderBrush" Value="White" />
                                <Setter Property="BorderBrush" Value="White" />
                                <Setter Property="FontFamily" Value="Bahnschrift SemiCondensed" />
                                <Setter Property="HorizontalContentAlignment" Value="Center" />
                                <Setter Property="Height" Value="20" />
                                <Setter Property="FontSize" Value="10" />
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.RowStyle>
                            <Style TargetType="{x:Type DataGridRow}">
                                <Setter Property="BorderBrush" Value="White" />
                                <Setter Property="BorderThickness" Value="1,1,1,1" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="Foreground" Value="White" />
                                <Setter Property="Height" Value="20" />
                                <Setter Property="FontFamily" Value="Bahnschrift SemiCondensed" />
                                <Setter Property="HorizontalContentAlignment" Value="Center" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>

                </DataTemplate>

            </Grid.Resources>


            <DataGrid x:Name="grades" ItemsSource="{Binding Subjects}" AutoGenerateColumns="False" HeadersVisibility="Column"
                      Width="1200" Background="Transparent" BorderBrush="White" Foreground="White">
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Предмет" Binding="{Binding Name}" Width="200" FontSize="17"/>
                    <DataGridTemplateColumn CellTemplate="{StaticResource FormulaTemplate}" Header="Элементы контроля" Width="600" />
                    <DataGridTextColumn Header="Credits" Binding="{Binding Credits}" Width="100"/>
                </DataGrid.Columns>

                <DataGrid.Resources>
                    <Style BasedOn="{StaticResource {x:Type DataGridColumnHeader}}" TargetType="{x:Type DataGridColumnHeader}">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderBrush" Value="White" />
                        <Setter Property="BorderBrush" Value="White" />
                        <Setter Property="FontFamily" Value="Bahnschrift SemiCondensed" />
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                        <Setter Property="Height" Value="50" />
                        <Setter Property="FontSize" Value="20" />
                    </Style>

                    <Style TargetType="{x:Type DataGridCell}">
                        <Style.Setters>
                            <Setter Property="TextBlock.TextAlignment" Value="Center" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                        </Style.Setters>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.RowStyle>
                    <Style TargetType="{x:Type DataGridRow}">
                        <Setter Property="BorderBrush" Value="White" />
                        <Setter Property="BorderThickness" Value="1,1,1,1" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="White" />
                        <Setter Property="Height" Value="80" />
                        <Setter Property="FontFamily" Value="Bahnschrift SemiCondensed" />
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                    </Style>
                </DataGrid.RowStyle>
                
                

            </DataGrid>
        </Grid>
    </Grid>
</Window>
