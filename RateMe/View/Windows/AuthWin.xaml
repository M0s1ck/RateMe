<Window x:Class="RateMe.View.Windows.AuthWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2"
        xmlns:userControls="clr-namespace:RateMe.View.UserControls"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen" Height="450" Width="500"
        Background="#10151b" MouseDown="OnWindowClick">

    <Window.Resources>
        
    </Window.Resources>
    
    
    <WindowChrome.WindowChrome>
        <WindowChrome />
    </WindowChrome.WindowChrome>

    <Grid x:Name="windowGrid">

        <Grid x:Name="MainGrid" Margin="0,60,0,0" HorizontalAlignment="Center">
            <StackPanel x:Name="LogInPanel" Orientation="Vertical">
                <userControls:DataHintTextBox DataContext="{ Binding EmailModel }" VerticalAlignment="Top" TextBoxFontSize="20" TextBoxHeight="34" TextBoxMinWidth="140" TextBlockFontSize="15" Margin="0,5"/>
                <userControls:DataHintTextBox DataContext="{ Binding PassModel }" VerticalAlignment="Top" TextBoxFontSize="20" TextBoxHeight="34" TextBoxMinWidth="140" TextBlockFontSize="15" Margin="0,5"/>
                <userControls:DataHintTextBox DataContext="{ Binding SecondPassModel }" VerticalAlignment="Top" TextBoxFontSize="20" TextBoxHeight="34" TextBoxMinWidth="140" TextBlockFontSize="15" Margin="0,5" x:Name="SecondPassForm"/>
            </StackPanel>

            <StackPanel VerticalAlignment="Center" Margin="0,150,0,0">
                <TextBlock Text="Or" FontSize="19" Foreground="White" FontFamily="Bahnschrift SemiCondensed" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10"/>
                <Button Style="{StaticResource ResourceKey=ContinueButtonStyle}" x:Name="FlipTaskButton" Click="OnFlipTaskClick" 
                     Content="{Binding _auTask}" VerticalAlignment="Center" FontSize="17"
                     FontFamily="Bahnschrift SemiCondensed" Height="42" Width="160" Foreground="White" Background="#422e52"/> <!--#4e4bf0-->
            </StackPanel>

        </Grid>
        
    </Grid>
</Window>
